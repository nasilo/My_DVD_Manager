<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>MyDvdManager</title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>


  <body>
    <nav class="top-bar small-12 columns" data-topbar role="navigation">
      <ul class="title-area small-3 columns" >
        <li class="name">
          <h1><a href="/dvds">MDVDM</a></h1>
        </li>
      </ul>

      <ul class="right">
        <% if user_signed_in? %>
          <li class='right-item nav-link sign-out'> <%= link_to 'Sign Out', session_path(current_user), :method => :delete %> </li>
        <% else %>
          <li class='right-item sign-in'> <%= link_to 'Sign In', signin_path %> </li>
        <% end %>
      </ul>
    </nav>

    <%= yield %>

  </body>
</html>
